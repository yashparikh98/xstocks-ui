import { KeyType } from "../interfaces";
export declare const GetOrSetTssDKGPubKey: (params: {
    endpoints: string[];
    verifier: string;
    verifierId: string;
    tssVerifierId: string;
    keyType?: KeyType;
}) => Promise<{
    key: {
        pubKeyX: string;
        pubKeyY: string;
        address: string;
        createdAt?: number;
    };
    isNewKey: boolean;
    nodeIndexes: number[];
}>;
