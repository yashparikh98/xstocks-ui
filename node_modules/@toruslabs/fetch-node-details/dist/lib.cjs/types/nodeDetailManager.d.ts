import { INodeDetails } from "@toruslabs/constants";
import { NodeDetailManagerParams } from "./interfaces";
declare class NodeDetailManager {
    private fndServerEndpoint;
    private _currentEpoch;
    private _keyType;
    private _sigType;
    private _torusNodeEndpoints;
    private _torusNodeRSSEndpoints;
    private _torusNodeSSSEndpoints;
    private _torusNodeTSSEndpoints;
    private _torusNodePub;
    private _torusIndexes;
    private updated;
    private network;
    constructor({ network, keyType, sigType, fndServerEndpoint, enableLogging, }?: NodeDetailManagerParams);
    get _nodeDetails(): INodeDetails;
    getNodeDetails({ verifier, verifierId }: {
        verifier: string;
        verifierId: string;
    }): Promise<INodeDetails>;
    getMetadataUrl(): Promise<string>;
    private setNodeDetails;
}
export default NodeDetailManager;
