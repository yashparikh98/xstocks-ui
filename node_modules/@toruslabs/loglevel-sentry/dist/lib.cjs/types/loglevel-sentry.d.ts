import { type addBreadcrumb, type captureException, type getActiveSpan, type getClient, type SeverityLevel } from "@sentry/core";
import { type Logger } from "loglevel";
export interface Sentry {
    addBreadcrumb: typeof addBreadcrumb;
    captureException: typeof captureException;
    getActiveSpan: typeof getActiveSpan;
    getClient: typeof getClient;
}
export declare class LoglevelSentry {
    private sentry?;
    private category;
    constructor(sentry?: Sentry);
    private static translateError;
    private static translateArgs;
    private static translateLevel;
    install(logger: Logger): void;
    setEnabled(enabled: boolean): void;
    isEnabled(): boolean;
    log(level: SeverityLevel, ...args: unknown[]): void;
    trace(...args: unknown[]): void;
    error(err: Error, ...args: unknown[]): void;
}
