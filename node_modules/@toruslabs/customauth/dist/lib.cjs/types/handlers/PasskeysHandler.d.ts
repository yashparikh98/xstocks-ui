import { CreateHandlerParams, LoginWindowResponse, TorusConnectionResponse } from "../utils/interfaces";
import AbstractLoginHandler from "./AbstractLoginHandler";
export default class PasskeysHandler extends AbstractLoginHandler {
    constructor(params: CreateHandlerParams);
    setFinalUrl(): void;
    getUserInfo(parameters: LoginWindowResponse, storageServerUrl?: string): Promise<TorusConnectionResponse>;
}
