import { CreateHandlerParams, ILoginHandler, LoginWindowResponse, TorusConnectionResponse } from "../utils/interfaces";
declare abstract class AbstractLoginHandler implements ILoginHandler {
    nonce: string;
    finalURL: URL;
    params: CreateHandlerParams;
    constructor(params: CreateHandlerParams);
    get state(): string;
    handleLoginWindow(params: {
        locationReplaceOnRedirect?: boolean;
        popupFeatures?: string;
    }): Promise<LoginWindowResponse>;
    abstract getUserInfo(params: LoginWindowResponse): Promise<TorusConnectionResponse>;
    abstract setFinalUrl(): void;
}
export default AbstractLoginHandler;
