import type { default as TypedEmitter } from "typed-emitter";
export type PopupHandlerEvents = {
    close: () => void;
};
declare const PopupHandler_base: new () => TypedEmitter<PopupHandlerEvents>;
export declare class PopupHandler extends PopupHandler_base {
    url: URL;
    target: string;
    features: string;
    window: Window;
    windowTimer: number;
    iClosedWindow: boolean;
    timeout: number;
    constructor({ url, target, features, timeout }: {
        url: URL;
        target?: string;
        features?: string;
        timeout?: number;
    });
    _setupTimer(): void;
    open(): Promise<void>;
    close(): void;
    redirect(locationReplaceOnRedirect: boolean): void;
}
export {};
