import { REDIRECT_PARAMS_STORAGE_METHOD_TYPE } from "./enums";
export declare class StorageHelper<T> {
    private currentStorageMethod;
    private isInitialized;
    private storageServerUrl;
    private localStorageAvailable;
    constructor(serverUrl: string);
    get storageMethodUsed(): REDIRECT_PARAMS_STORAGE_METHOD_TYPE;
    init(): void;
    storeData(key: string, params: T): Promise<void>;
    retrieveData(key: string): Promise<T>;
    clearStorage(key: string): void;
    clearOrphanedData(baseKey: string): void;
}
