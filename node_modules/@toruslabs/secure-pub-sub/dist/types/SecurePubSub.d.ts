import { LogLevelDesc } from "loglevel";
import { type Socket } from "socket.io-client";
import { ISecurePubSubOptions } from "./interfaces";
export declare class SecurePubSub {
    options: ISecurePubSubOptions;
    SOCKET_CONN: Socket | null;
    constructor(options?: ISecurePubSubOptions);
    static setLogLevel(level: LogLevelDesc): void;
    publish(topic: string, message: string): Promise<void>;
    subscribe(topic: string): Promise<string>;
    cleanup(): void;
    private getSocketConnection;
}
