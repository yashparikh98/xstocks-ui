import { type IdleTimeTracker } from "./idleTimeTracker";
export type PollingAction = () => Promise<void> | void;
export declare class PollingManager {
    private idleTimeTracker;
    private pollingInterval;
    private isPolling;
    constructor(idleTimeTracker: IdleTimeTracker, pollingInterval: number);
    start(action: PollingAction): void;
    stop(): void;
    private poll;
}
