import { PriorityQueue } from '../../lib/priority-queue';
import { Context } from '../context';
import { Plugin } from '../plugin';
import { CoreEventQueue } from '@segment/analytics-core';
export declare class EventQueue extends CoreEventQueue<Context, Plugin> {
    constructor(name: string);
    constructor(priorityQueue: PriorityQueue<Context>);
    flush(): Promise<Context[]>;
}
//# sourceMappingURL=event-queue.d.ts.map