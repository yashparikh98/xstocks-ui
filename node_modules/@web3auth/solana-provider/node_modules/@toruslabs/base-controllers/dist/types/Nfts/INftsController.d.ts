import { BaseConfig, BaseState, IController } from "../interfaces";
export type CustomNftItemInfo = {
    image: string;
    name: string;
    tokenBalance: string;
    description: string;
    tokenId: string;
    video?: string;
    decimals?: string;
    customNftId?: string;
};
export type CustomNftInfo = {
    contractAddress: string;
    contractName: string;
    contractSymbol: string;
    contractImage?: string;
    contractSupply?: string;
    contractFallbackLogo?: string;
    nftStandard: string;
    contractDescription?: string;
    chainId: string;
    assets: CustomNftItemInfo[];
};
export interface NftsControllerConfig extends BaseConfig {
    interval?: number;
    selectedAddress: string;
    chainId: string;
}
export interface NftsControllerState extends BaseState {
    nfts: Record<string, CustomNftInfo[]>;
}
export interface INftsController<C, S> extends IController<C, S> {
    refreshNftBalances(): Promise<void>;
}
