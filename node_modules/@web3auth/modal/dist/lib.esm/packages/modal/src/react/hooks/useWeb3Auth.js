import { useWeb3AuthInner } from './useWeb3AuthInner.js';

const useWeb3Auth = () => {
  const {
    initError,
    isConnected,
    isAuthorized,
    isInitialized,
    isInitializing,
    provider,
    status,
    web3Auth,
    getPlugin
  } = useWeb3AuthInner();
  return {
    initError,
    isConnected,
    isAuthorized,
    isInitialized,
    isInitializing,
    provider,
    status,
    web3Auth,
    getPlugin
  };
};

export { useWeb3Auth };
