import { useWeb3AuthInner } from './useWeb3AuthInner.js';

const useChain = () => {
  const {
    chainId,
    chainNamespace
  } = useWeb3AuthInner();
  return {
    chainId,
    chainNamespace
  };
};

export { useChain };
