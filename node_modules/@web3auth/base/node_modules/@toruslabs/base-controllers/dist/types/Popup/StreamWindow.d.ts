import { BaseController } from "../BaseController";
import { IWindow } from "../interfaces";
import { StreamWindowConfig, StreamWindowState } from "./interfaces";
export declare class StreamWindow extends BaseController<StreamWindowConfig, StreamWindowState> implements IWindow {
    closed: boolean;
    constructor({ config, state, }: {
        config: Partial<StreamWindowConfig> & Pick<StreamWindowConfig, "communicationEngine" | "communicationWindowManager">;
        state: Partial<StreamWindowState> & Pick<StreamWindowState, "windowId">;
    });
    open(): Promise<this>;
    close(): void;
}
