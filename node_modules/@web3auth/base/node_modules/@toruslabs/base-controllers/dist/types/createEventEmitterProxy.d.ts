import { SafeEventEmitter } from "@web3auth/auth";
interface EventEmitterProxyOpts {
    eventFilter?: ((name: string) => boolean) | string;
}
export declare function createEventEmitterProxy<T extends SafeEventEmitter>(initialTarget: T, opts?: EventEmitterProxyOpts): T;
export {};
