import { Chain } from "viem";
import { BundlerClient, SmartAccount } from "viem/account-abstraction";
import { IProvider } from "../../../base";
import { IEthProviderHandlers } from "../../ethereum-provider";
export declare function getProviderHandlers({ bundlerClient, smartAccount, chain, eoaProvider, }: {
    smartAccount: SmartAccount;
    bundlerClient: BundlerClient;
    chain: Chain;
    eoaProvider: IProvider;
}): IEthProviderHandlers;
